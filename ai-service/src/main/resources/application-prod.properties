eureka.client.service-url.defaultZone=${EUREKA_URL}
spring.ai.openai.api-key=${AI_API_KEY}
spring.ai.openai.chat.options.model=${AI_MODEL:cohere/north-mini-code:free}
spring.ai.openai.base-url=${AI_BASE_URL:https://openrouter.ai/api/v1}

logging.level.root=WARN
logging.level.com.ehtesham.ai_service=INFO

springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# RS256: this service only ever VERIFIES tokens (public key) — it
# cannot mint one. Must be the public counterpart of securebank-api's
# jwt.private-key.
jwt.public-key=${JWT_PUBLIC_KEY}
